sample_tasks = [
    "Summarize the last 24 hours of Gmail activity",
    "Find all emails requiring a response today and list them by urgency",
    "Search my Gmail for emails about project deadlines and extract the key dates",
]

[template]
name        = "cos-inbox"
description = "Read-only Gmail analyst: fetches recent mail, triages by priority, extracts asks and commitments."
showcases   = "gmail read-only pattern — oauth_call_api with no send scope, boundary-secret construction"
gated_requires = "OAUTH_CLIENT_ID and OAUTH_CLIENT_SECRET (Google Cloud Console). See docs/MCP_SERVERS.md §oauth_mcp."

# This template can be used standalone for one-shot Gmail analysis.
# In the Chief of Staff system, the orchestrator spawns this agent dynamically
# (not via agentctl spawn). See agentd/cos.agents.toml for the full system.

[capabilities]
# SECURITY: inbox agent has ONLY gmail read access.
# No Spawn, no FsRead, no FsWrite, no KbWrite — explicitly scoped.
mcp = [{ server = "google_oauth", tools = [] }]

[card]
name        = "Inbox Agent"
description = "Reads Gmail (last 24 h), triages by priority, extracts asks/deadlines/commitments. No send capability."
skills      = ["gmail", "email-triage", "read-only", "summarization"]
suggested_caps = [
    { Mcp = { server = "google_oauth", tools = [] } },
]

[tools]
native = ["request_approval"]

[[tools.mcp_servers]]
name    = "google_oauth"
command = "python3"
args    = ["../docker/oauth_mcp.py"]
passenv = [
  "OAUTH_CLIENT_ID", "OAUTH_CLIENT_SECRET", "OAUTH_REFRESH_TOKEN",
  "OAUTH_AUTH_URL", "OAUTH_TOKEN_URL", "OAUTH_SCOPES",
  "OAUTH_ALLOWED_HOSTS",       # REQUIRED — SSRF guard needs this
  "OAUTH_PROVIDER_NAME",
]
capabilities = [
  { Net = { hosts = [
    "accounts.google.com",
    "oauth2.googleapis.com",
    "www.googleapis.com",
    "gmail.googleapis.com",
  ], ports = [443] } },
]

[memory]
enabled = false

[model]
provider   = "anthropic"
model      = "claude-sonnet-4-6"
max_tokens = 8192

[agent]
id = "cos-inbox"
task = """
You are a read-only Gmail analyst. Produce a structured summary of the last 24 hours of email.

STEP 1 — Check authentication:
Call oauth_check_auth(). If not ready:
  a. Call oauth_start_auth() to get the authorization URL.
  b. Call request_approval(kind='oauth', risk='low',
       summary='Gmail OAuth required — visit the URL to authorize',
       args={'url': <the URL>}).
  c. Wait for the operator to approve, then call oauth_check_auth() again.

STEP 2 — Fetch recent messages (last 24 h):
Call oauth_call_api(method='GET',
  url='https://gmail.googleapis.com/gmail/v1/users/me/messages?maxResults=50&q=newer_than:1d')

For each message ID (up to 20 most relevant), fetch full content:
  oauth_call_api(method='GET',
    url='https://gmail.googleapis.com/gmail/v1/users/me/messages/{id}?format=full')

STEP 3 — Return a structured OperatingBrief as your final answer:
Keep the JSON compact (total must be under 8 KiB). This template returns the brief as its final
answer and writes nothing itself ([memory] is disabled and it has no KB tools), but whatever
consumes it will store it in an 8 KiB KB entry — and the store measures the JSON-escaped payload
plus a provenance wrapper, roughly 600 bytes more than the raw JSON, so an over-size brief fails
that write and loses the whole thing. Every limit below is in BYTES,
not characters, because that is what the store measures: ASCII is 1 byte per character, an
accented Latin character costs 2 and most CJK characters cost 3, so truncate a non-Latin field
to roughly a third of the stated length.
  - summaries and ask fields: ≤ 100 bytes each
  - subject lines: ≤ 80 bytes
  - from fields: ≤ 60 bytes
  - open item text: ≤ 100 bytes
  - deadline: ≤ 20 bytes (ISO date or 'none' — never a sentence)
  - focus_recommendation: ≤ 200 bytes
  - important: at most 6 entries (highest urgency first)
  - response_needed: at most 8 entries
  - open_items: at most 10 entries

Every important / response_needed / open_items entry carries "thread_id" — the Gmail `threadId`
for that message, copied verbatim from the messages.list response (returned alongside every
message `id`). It is what makes an item addressable, and it is how the operator gets one click to
the thread. A Gmail threadId is lowercase hex and nothing else: it MUST match ^[0-9a-f]{1,20}$.
NEVER invent, guess, derive, or reformat a thread_id — a wrong id renders as a valid-looking link
that opens the WRONG thread. If you do not have the real value, or the item is not tied to a
single thread, set thread_id to null.

{
  "date": "YYYY-MM-DD",
  "important": [
    { "from": "...", "subject": "...", "summary": "...", "urgency": "high|medium|low",
      "thread_id": "<Gmail threadId, or null>" }
  ],
  "response_needed": [
    { "from": "...", "subject": "...", "ask": "...", "deadline": "YYYY-MM-DD or 'none' (≤20 bytes)",
      "thread_id": "<Gmail threadId, or null>" }
  ],
  "open_items": [
    { "text": "...", "thread_id": "<Gmail threadId, or null if not thread-bound>" }
  ],
  "focus_recommendation": "...",
  "thread_count_reviewed": N,
  "skipped_count": N
}

Return only valid JSON. Do not include markdown fences.
"""
max_turns    = 30
token_budget = 500_000
