# Negative-control fixture for config_parse_all (audit.1).
# Reproduces the P0-1 class: `model_id` is not a ModelConfig field (the real key is
# `model`) and ModelConfig is #[serde(deny_unknown_fields)] — parsing must fail.
# This file lives in tests/fixtures/, OUTSIDE the directories the positive test globs.

[agent]
id = "broken-fixture"
task = "never runs"

[model]
provider = "anthropic"
model_id = "claude-haiku-4-5-20251001"
